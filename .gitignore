node_modules/
*.tgz
# a library: the consumer resolves its own versions
bun.lock
package-lock.json
pnpm-lock.yaml
yarn.lock
# amxts prepare: the editor config the playground extends
.amxts/
# amxts build in the playground
dist/
